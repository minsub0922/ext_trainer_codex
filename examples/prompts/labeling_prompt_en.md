# English IE Labeling Prompt Example

Read the document and return JSON only.

- Use only the provided schema.
- Keep missing values as `null`.
- Do not infer facts that are not explicitly supported by the text.
- For entity and relation tasks, return arrays even when they are empty.
