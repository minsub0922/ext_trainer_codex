"""Human-readable extension notes."""

from __future__ import annotations


OLMO3_NOTES = [
    "This path is a PoC for future extensibility, not the default training lane.",
    "Most repo components remain model-agnostic because the canonical schema is the source of truth.",
    "Model-specific work is isolated to config selection, prompt template choice, and tokenizer/runtime checks.",
]
